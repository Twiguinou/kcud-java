package showoff.App.Demo;

import showoff.App.DemoInterface;
import showoff.App.Render.Camera;

public class BasicDemo implements DemoInterface
{
    @Override
    public void setupScene(Camera camera)
    {
    }
}
